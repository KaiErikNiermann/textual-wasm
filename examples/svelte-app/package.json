{
  "name": "svelte-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Textual app mounted inside a Svelte component.",
  "scripts": {
    "terminal": "poetry run textual-wasm build palette_app.app:Palette palette_app -o public/terminal --title Palette",
    "dev": "pnpm terminal && vite",
    "build": "pnpm terminal && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "svelte": "^5.42.2",
    "vite": "^7.1.9"
  }
}
